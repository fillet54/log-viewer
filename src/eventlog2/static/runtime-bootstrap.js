import * as preact from "preact";
import * as hooks from "preact/hooks";
import * as signals from "preact/signals";
import htm from "htm";

window.preact = preact;
window.preactHooks = hooks;
window.preactSignals = signals;
window.htm = htm;
